"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { authClient } from "@/lib/auth-client";
export default function SignUpPage(){const router=useRouter();const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await authClient.signUp.email({email:String(f.get("email")),password:String(f.get("password")),name:"Juice Box Admin"});if(r.error)setError("This account could not be created. Use the approved studio email.");else{router.push("/");router.refresh()}}return <main className="flex min-h-screen items-center justify-center bg-background px-6 text-foreground"><form onSubmit={submit} className="flex w-full max-w-md flex-col gap-5 border border-primary/30 bg-card p-8"><h1 className="cinzel text-3xl font-black">Create admin account</h1><p className="text-sm text-muted-foreground">This setup is restricted to the studio owner email.</p><input name="email" type="email" placeholder="kalebmay18@gmail.com" required className="h-11 border border-white/15 bg-background px-3"/><input name="password" type="password" minLength={8} required placeholder="Password" className="h-11 border border-white/15 bg-background px-3"/>{error&&<p role="alert" className="text-sm text-destructive">{error}</p>}<button className="h-12 bg-primary font-bold uppercase tracking-widest text-primary-foreground">Create account</button></form></main>}
